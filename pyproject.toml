[project]
name = "zk-docs"
version = "0.1.0"
description = "Documentation for zk"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "zensical>=0.0.40",
]
